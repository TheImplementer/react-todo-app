import React, { ChangeEvent } from 'react';
import './App.css';
import Header from './Header';

const App: React.FC = () => {
  return (
    <div className="app-container">
      <Header />
    </div>
  );
};

export default App;
